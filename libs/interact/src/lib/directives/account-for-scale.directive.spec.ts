import { AccountForScaleDirective } from './account-for-scale.directive';

describe('AccountForScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountForScaleDirective();
    expect(directive).toBeTruthy();
  });
});
