import { DragRootDirective } from './drag-root.directive';

describe('DragRootDirective', () => {
  it('should create an instance', () => {
    const directive = new DragRootDirective();
    expect(directive).toBeTruthy();
  });
});
