import { ApplyScaleDirective } from './apply-scale.directive';

describe('ApplyScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyScaleDirective();
    expect(directive).toBeTruthy();
  });
});
