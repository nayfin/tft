import { InitialControlValuePipe } from './initial-control-value.pipe';

describe('InitialControlValuePipe', () => {
  it('create an instance', () => {
    const pipe = new InitialControlValuePipe();
    expect(pipe).toBeTruthy();
  });
});
